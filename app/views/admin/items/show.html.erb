<h4 class ="shadow p-3 mb-5 bg-light rounded font-weight-bold font-italic bg-dark text-white">商品詳細</h4>
<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-3">
      <%= image_tag @item.get_image(300, 200) %>
    </div>
    <div class='col-md-8 offset-md-1'>
      <table class="table">
        <tbody>
          <tr>
          <th>商品名</th>
          <td><%= @item.name %></td>
          </tr>
          <tr>
          <th>商品説明</th>
          <td><%= @item.introduction %></td>
          </tr>
          <tr>
          <th>ジャンル</th>
          <td><%= @item.genre.name %></td>
          </tr>
          <tr>
          <th>税込価格<br>(税抜価格)</th>
          <td><%= (1.1*(@item.price_no_tax)).round.to_s(:delimited) %>
              (<%= @item.price_no_tax.to_s(:delimited) %>)円
          </td>
          </tr>
          <tr>
          <th>販売ステータス</th>
          <td>
            <% if @item.sales_status %>
              <p class="text-success" >販売中</p>
            <% else %>
              <p class="text-secondary">販売停止中</p>
            <% end %>
          </td>
          </tr>
        </tbody>
      </table>
      <div>
        <%= link_to "編集する", edit_admin_item_path(@item), class: 'btn btn-success' %>
      </div>
    </div>
  </div>
</div>